# test-project

## 概要

このリポジトリは `keidai-rdo/test-project` のプロジェクトです。

## 始め方

### 前提条件

- Git がインストールされていること

### リポジトリのクローン

```bash
git clone <リポジトリのURL>
cd test-project
```

## ブランチ戦略

- `main` / `master`: 本番環境用の安定ブランチ
- `claude/*`: AI によって作成されたフィーチャーブランチ

## コントリビューション

1. フィーチャーブランチを作成する (`git checkout -b feature/your-feature`)
2. 変更をコミットする (`git commit -m 'Add some feature'`)
3. ブランチにプッシュする (`git push origin feature/your-feature`)
4. プルリクエストを作成する

## ライセンス

このプロジェクトのライセンスについては、リポジトリ管理者にお問い合わせください。
